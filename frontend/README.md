# React Dashboard (frontend)

This is a minimal Vite + React frontend that talks to the Flask backend at `/api/*`.

Install and run:

```bash
cd frontend
npm install
npm run dev
```

The frontend expects the Flask server to run at `http://localhost:5000` (same origin or use a proxy).
